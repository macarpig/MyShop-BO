<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.daoko.mapper.memberMapper">
	<!-- 사용자 목록 -->
	<select id="listmember" resultType="kr.daoko.dto.MemberDTO">
		select userId, userName, userTel, userAddr1, userAddr2, userRank, userAuth
		from member
	</select>
	
	<!-- ID에 의해 회원 정보 조회 -->
	<select id="memberInfo" parameterType="String" resultType="kr.daoko.dto.MemberDTO">
		select userId, userName, userEmail, userTel, userAddr1, userAddr2, userRank, userAuth
		from member
		where userId = #{userId}
	</select>
</mapper>