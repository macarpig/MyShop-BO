<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.daoko.mapper.orderMapper">
	<!-- 주문 조회 -->
	<select id="orderInquiry" resultType="kr.daoko.dto.OrderDTO">
		select o.orderId, o.orderDate, o.userId, m.userrank, sum(g.gdsPrice*d.cartStock) as totalPrice, o.orderStatus as orderStatus
		from ordertbl o, orderDetail d, member m, goods g
		where o.orderId = d.orderId and o.userId = m.userId and d.gdsCode = g.gdsCode
		group by o.orderId 
	</select>
</mapper>